version: '3'
services:
  # code-clx-go:
  #   build: go/code
  #   volumes:
  #     - ./.vscode/.vscode-server/:/root/.vscode-server/
  #     - ./.vscode/.go:/go
  #     - ./go:/code
  #   ports:
  #     - 127.0.0.1:35882:8000
  build:
    build: go
    volumes:
      # got build result
      - ./go/bin/:/src/go/bin:wo
    # volumes:
    #   - ./.vscode/.go:/go
    #   - ./go:/code
